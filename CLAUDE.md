# elevator-gurl - 開発メモ

PixiJS 8 + TypeScript + Vite のスマホ縦比率 (9:16, 360x640) エレベーターゲーム。
プロジェクト構造は `amanuma` の雛形をベースにしている。

- `src/main.ts`: エントリーポイント。Title / Play / Result の 3 シーンを SceneManager に登録
- `src/scenes/`: 各シーン (TitleScene / PlayScene / ResultScene) + SceneManager (カメラ tween)
- `src/input/`: KeyboardManager / TouchManager (両方とも amanuma からそのまま流用)
- `src/audio/`: SoundManager / MuteButton (localStorage キーは `elevator_gurl_muted`)
- `src/constants/colors.ts`: UI 色のみ (落ち物パズル用のブロック色は持ち込まない)
- `src/game/types.ts`: GameState / Resident / Walker / ElevatorState の型定義
- `src/game/logic.ts`: ピュア関数のゲームロジック (PixiJS 非依存)

詳細仕様は notes リポの `notes/dev/elevator-gurl.md` 側で管理する。

## ゲームフロー概要

チューリン（主人公）はエレベータに常に乗っており、エレベータと一緒に上下する。

### エレベータの動作

1. 1階に戻ると待機客が乗り込む（上限10人）
2. エレベータは上昇しながら順番に各階に止まり、その階の客を降ろす
3. 全員届けたら1階に戻り繰り返す

### 客の種類

- **1階待機客**: 各自の居住階へ（プレイヤーが行き先を覚えて正しい階で止める）
- **上階乗車客**: 全員1階行き（固定、行き先は覚えなくてよい）
  - ただし「どの階から乗ってきたか = その人の居住階」は重要
  - 乗降のタイミングで必ず名前・顔・階数を表示 → プレイヤーが覚える
  - 次に1階で同じ人に会ったとき、覚えた居住階に届けるのがゲームの核心

### 正誤判定

- 1階で待機客が乗り込んだ後、出発前にプレイヤーが各客の行き先ボタンを押す
- 各ボタンには「誰が（誰のために）押したか」を名前・顔で表示する
- 押せなかった客: 客自身が違う色でボタンを押す（名前・顔も表示、教示）
- エレベータはボタンが押された階に上昇しながら順番に止まり、各客を降ろす
- 正解: 通常通り届ける
- 不正解 / 未入力: 客がガッカリし、客自身が正しい階を違う色で押して降りる

### ボタンパネルのUI

- 基本: ビル断面の各階に対応したボタンを縦に並べる（階と1:1対応）
- 階数が多い場合: 一定以上は吹き出し形式でオーバーレイ表示
- 各ボタンに押した人の名前を表示（プレイヤー押し=通常色、客自押し=違う色）

### 待機上限

- 1階の待機客は最大10人
