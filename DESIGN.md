# DESIGN.md — ヱレベヰターガール

## 1. Vision

**九龍城の夜に生きる少女チューリンの記憶** を、エレベーターというミクロな舞台で体験させる。

- 朝に出勤・通学で出ていく住民、夜に疲れて帰ってくる住民。時刻の変化と生活感がビジュアルで伝わる
- 画面は常に薄暗く、ネオンと電球色だけが光る。日中でも太陽は見えない（九龍城は採光がない）
- プレイヤーはチューリンとして「覚える」ゲームをする。住民の顔と階を記憶することが核心

---

## 2. Design Tokens

### 2-1. カラーパレット

#### 時刻パレット（背景・環境色）

時刻に応じて背景とライティング色が変化する。コードでは `TimeOfDay` enum で管理する。

| 時刻   | 英名       | 背景色    | 環境光    | 用途                         |
| ------ | ---------- | --------- | --------- | ---------------------------- |
| 深夜   | `midnight` | `#050308` | `#1a0a2e` | 00:00〜05:00                 |
| 夜明け | `dawn`     | `#0d0a1a` | `#3d1c52` | 05:00〜07:00                 |
| 朝     | `morning`  | `#1a1008` | `#7a4a10` | 07:00〜11:00（出勤ラッシュ） |
| 昼     | `noon`     | `#1a1208` | `#5a3a18` | 11:00〜15:00（閑散）         |
| 夕方   | `evening`  | `#0f0804` | `#8a3010` | 15:00〜19:00（帰宅ラッシュ） |
| 夜     | `night`    | `#080510` | `#0a0a2a` | 19:00〜24:00                 |
| 雨     | `rain`     | `#060810` | `#1a2a3a` | 任意時刻に重ねる             |

> **九龍城原則**: 直射日光は差し込まない。「朝」でも暗い。電灯と隙間から漏れる光だけ。

#### UIカラー

```
UI_BG            = #0f0f1a  // アプリ背景
UI_PRIMARY       = #7c3aed  // メインアクション（スタートボタン等）
UI_SECONDARY     = #06b6d4  // ホバー・ハイライト
UI_TEXT_PRIMARY  = #ffffff  // 本文テキスト
UI_TEXT_DIM      = #aaaaaa  // 補助テキスト

// ビル・エレベーター
COLOR_BUILDING_BG  = #1a1208  // ビル断面背景（煤けた壁）
COLOR_FLOOR_LINE   = #4a3820  // 床ライン（古い木の床）
COLOR_SHAFT_BG     = #0d0d1a  // シャフト（暗い縦穴）
COLOR_ELEV_BODY    = #c8a85a  // エレベーター箱（真鍮・くすんだ金）
COLOR_ELEV_DOOR    = #8b6914  // ドア縦線

// ボタン状態
COLOR_BTN_EMPTY    = #2a1f0e  // 未押し（暗い木目）
COLOR_BTN_PLAYER   = #7c3aed  // プレイヤーが押した（正解）
COLOR_BTN_SELF     = #aa2222  // 客自身が押した（不正解）
COLOR_BTN_AUTO     = #1a4a2a  // 上の階から自動（1F行き）

// ログ・待機列
COLOR_WAITING_TEXT = #ddcc88  // 待機客名前（電球色）
COLOR_WAITING_MORE = #888866  // 「...他N人」
COLOR_LOG_TEXT     = #aaccff  // 乗降ログ（青白い蛍光灯）
```

#### ネオン・看板アクセントカラー（将来の背景装飾用）

```
NEON_RED     = #ff2244  // 赤ネオン（飲食店）
NEON_YELLOW  = #ffcc00  // 黄ネオン（薬局）
NEON_CYAN    = #00ddcc  // 青緑ネオン（バー）
NEON_ORANGE  = #ff6600  // 橙ネオン（麻雀）
LAMP_WARM    = #ffaa44  // 電球色（廊下・室内）
```

### 2-2. タイポグラフィ

| 用途                          | フォント    | サイズ   | 備考                   |
| ----------------------------- | ----------- | -------- | ---------------------- |
| ゲームUI（HUD・ログ・ボタン） | `monospace` | 9〜13px  | ドットフォント感覚     |
| タイトルロゴ「囡」            | システムUI  | 96px     | 漢字の力強さを活かす   |
| 副題「ヱレベヰターガール」    | システムUI  | 18px     | カタカナ旧字体で時代感 |
| 住民名（漢字）                | `monospace` | 10〜12px | nameZh を優先表示      |

---

## 3. Layout — PlayScene (360×640)

### 3-1. 現状の配置

```
[ 0, 0 ]────────────────────── 360px ─┐
│  HUD (高さ48px)                       │ ← スコア・ミス・フェーズ
├──────────────────────────────────────┤
│ シャフト(36px) │ ビル断面 (316px)       │
│               │                       │
│  [エレベ]      │  [8F] [ボタン] 名前    │
│               │  [7F] [ボタン]         │
│  ↑            │  ...                  │
│  (上下移動)   │  [2F] [ボタン]         │
│               │  [1F] 待機列  ログ     │
├──────────────────────────────────────┤
│ 画面下端 (y=640)                       │
└───────────────────────────────────────┘
```

### 3-2. 課題と改善方針

#### 課題

1. **HUD が情報不足**: 時刻・天気・住民感情・現金がない
2. **1階エリアが狭い**: 待機列とログが同じエリアに詰め込まれている
3. **シャフトが細すぎる（36px）**: チューリンの存在感が乏しい
4. **窓の位置が固定**: 時刻によるライティング変化を受けない
5. **ビル右側の空白**: ボタン右の名前エリアが常に余っている

#### 改善方針（段階的）

**Phase 1（現在フェーズ）**: 情報を整理して読みやすくする

- HUD を2行化: 上段=スコア・ミス、下段=時刻アイコン・フェーズ
- 待機列を1Fボタンエリア左、ログを右と明確に分離

**Phase 2（時刻実装後）**: 時刻に応じた背景色変化

- `TimeOfDay` を GameState に追加し、1ゲーム内で朝〜夜まで変化
- ビル背景グラデーション（上=暗い空、下=地面）を時刻連動で変化
- 窓から漏れる灯りを時刻で制御（夜は明るい、昼は暗い）

**Phase 3（ドット絵素材）**: 九龍城の背景を本素材に差し替え

- 背景を差し替えやすいよう `buildingGfx` を背景レイヤーと前景レイヤーに分離済み

---

## 4. Components

### 4-1. エレベーター箱

- サイズ: 幅 `ELEV_W = 28px`、高さ `FLOOR_H * 0.82`
- 色: くすんだ真鍮 `#c8a85a`、ドア縦線 `#8b6914`
- チューリンの人影: 頭（肌色円）＋ 体（青の矩形）でシルエット表現
- 乗客数インジケータ: エレベーター箱内に小数字（0人のとき非表示）
- 扉が開いているとき: 床ライン全体を `COLOR_DOOR_OPEN` でうっすらハイライト

### 4-2. ボタンパネル

- 各階に1つのボタン（2F〜FLOOR_COUNT）
- 状態: `empty` / `player` / `self` / `auto`
- 「誰が押したか」を nameZh でボタン右に表示
- inputフェーズ中はタイマーバー（緑→赤）を HUD 直下に表示

### 4-3. 待機列（1Fエリア）

- waitingQueue の先頭10人を nameZh で縦一列
- 表示可能行を超えたら「...他N人」
- 乗り込み演出中は全員「＞」で移動中を示す（将来）

### 4-4. 乗降ログ

- door_open 終了後に記録
- `「NF: 陳小明 ↓」` / `「NF: 李芳 ↑1F」` 形式
- 最大3件、古いものを押し出し
- 1Fエリア右側（NAME_X 以降）に表示

### 4-5. HUD（上段バー）

現在: `○ N × N` + フェーズテキスト

将来追加予定:

- **時刻アイコン**: 🌙 🌅 ☀️ 🌆 🌃 ☔ の6状態（または ASCII 文字列 `[夜]` `[朝]` 等）
- **現金**: `¥ NNN` 形式
- **住民満足度**: ハートゲージまたは数値

---

## 5. Time-of-Day System（将来実装）

### 時刻とゲームプレイの関係

| 時刻                 | 住民の動き               | ゲーム的効果               |
| -------------------- | ------------------------ | -------------------------- |
| 深夜                 | ほぼ誰も動かない         | 閑散、スコアを稼ぎにくい   |
| 夜明け               | 朝早い住民が出始める     | 少数、覚えやすい           |
| **朝（出勤・通学）** | **一斉に1Fへ降りてくる** | **ラッシュ、ミスしやすい** |
| 昼                   | 老人・主婦が散発的に動く | 穏やか                     |
| **夕方（帰宅）**     | **一斉に上の階へ戻る**   | **ラッシュ、ミスしやすい** |
| 夜                   | 遅い人が帰ってくる       | 少数、稼ぎやすい           |
| 雨                   | 外出者が減る             | 閑散                       |

### 実装イメージ

```ts
// types.ts に追加予定
export type TimeOfDay =
  | 'midnight'
  | 'dawn'
  | 'morning'
  | 'noon'
  | 'evening'
  | 'night'
export type Weather = 'clear' | 'rain'

// GameState に追加予定
gameTimeMinutes: number // 0〜1440（1日分。1ゲーム=24時間サイクル）
weather: Weather
```

ビル背景色は `gameTimeMinutes` に応じて線形補間する。

---

## 6. Do / Don't

| Do                                   | Don't                                |
| ------------------------------------ | ------------------------------------ |
| 暗く、電灯色だけで照らす             | 鮮やかな背景色（明るすぎる）         |
| nameZh（漢字）を優先表示             | アルファベット名を大きく出す         |
| モノスペースフォントで統一           | プロポーショナルフォントを UI に混在 |
| 時刻で色をゆっくり変化させる         | 突然背景色が切り替わる               |
| ボタン状態を色だけでなく名前でも示す | 色だけで正誤を判断させる             |

---

## 7. Roadmap

| Phase       | 内容                                         | 対応 Issue |
| ----------- | -------------------------------------------- | ---------- |
| ✅ Phase 0  | 雛形・シーン遷移                             | #10 #11    |
| ✅ Phase 1a | ビル描画・エレベーター移動                   | #12 #13    |
| ✅ Phase 1b | 待機列UI・乗降ログ・乗客数                   | #14        |
| ✅ Phase 1c | ゲームオーバー自動遷移・スコア表示           | #7         |
| 🔲 Phase 2a | HUD 充実（時刻アイコン・現金・満足度）       | #19        |
| 🔲 Phase 2b | 時刻システム実装・背景色変化                 | 新規起票   |
| 🔲 Phase 2c | キャラ別イベント（老人・子供）               | #18        |
| 🔲 Phase 3  | タイトル装飾（チューリン横顔・ドット絵背景） | #20 #21    |
| 🔲 Phase 4  | ドット絵本素材への差し替え                   | 新規起票   |
